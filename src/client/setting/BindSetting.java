package client.setting;

public final class BindSetting extends KeybindSetting
{
    public BindSetting(String key, String name, String description, int defaultKeyCode, BindMode defaultMode)
    {
        super(key, name, description, defaultKeyCode, defaultMode);
    }
}
